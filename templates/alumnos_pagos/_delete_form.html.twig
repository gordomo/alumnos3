<form method="post" action="{{ path('app_alumnos_pagos_delete', {'id': alumnos_pago.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ alumnos_pago.id) }}">
    <button class="btn">Delete</button>
</form>
