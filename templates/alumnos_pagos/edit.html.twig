{% extends 'base.html.twig' %}

{% block title %}Edit AlumnosPagos{% endblock %}

{% block body %}
    <div class="editForms">
        <h1>Editar Pago</h1>
        <div class="row">
            {{ include('alumnos_pagos/_form.html.twig', {'button_label': 'Guardar'}) }}
            {{ include('alumnos_pagos/_delete_form.html.twig') }}
        </div>
        <br><br>
        <div class="row botones">
            <div class="col-md-12">
                <a class="btn btn-primary" href="{{ path('app_alumnos_pagos_index') }}">Volver</a>
            </div>
        </div>
    </div>

{% endblock %}
