{% extends 'base.html.twig' %}

{% block title %}AlumnosPagos index{% endblock %}

{% block body %}
    <h1>Pagos {{ alumno }}</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Fecha</th>
                <th>Monto</th>
                <th>Mes</th>
                <th>Año</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for alumnos_pago in alumnos_pagos %}
            <tr>
                <td>{{ alumnos_pago.fecha ? alumnos_pago.fecha|date('Y-m-d') : '' }}</td>
                <td>{{ alumnos_pago.monto }}</td>
                <td>{{ alumnos_pago.mes }}</td>
                <td>{{ alumnos_pago.ano }}</td>
                <td>
                    <a class="btn" target="_blank" href="{{ path('app_alumnos_pagos_show', {'id': alumnos_pago.id}) }}">
                        <i class="bi bi-printer"></i>
                    </a>
                    <a class="btn" href="{{ path('app_alumnos_pagos_edit', {'id': alumnos_pago.id}) }}">
                        <i class="bi bi-pencil"></i>
                    </a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <a class="btn btn-primary" href="javascript:history.back()">Volver</a>
    {#<a href="{{ path('app_alumnos_pagos_new') }}">Create new</a>#}
{% endblock %}
