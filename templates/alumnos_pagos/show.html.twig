{% extends 'base.html.twig' %}

{% block title %}AlumnosPagos{% endblock %}

{% block body %}
    <h1>AlumnosPagos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ alumnos_pago.id }}</td>
            </tr>
            <tr>
                <th>Fecha</th>
                <td>{{ alumnos_pago.fecha ? alumnos_pago.fecha|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_alumnos_pagos_index') }}">back to list</a>

    <a href="{{ path('app_alumnos_pagos_edit', {'id': alumnos_pago.id}) }}">edit</a>

    {{ include('alumnos_pagos/_delete_form.html.twig') }}
{% endblock %}
