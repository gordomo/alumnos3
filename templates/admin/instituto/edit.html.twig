{% extends 'base.html.twig' %}

{% block title %}Editar {{instituto.nombre}}{% endblock %}

{% block body %}
<div class="container mt-3 d-flex justify-content-center">
    <div class="col-lg-12">
        <h1 class="mb-4 text-center">Editar {{instituto.nombre}}</h1>
        <div class="card shadow-sm">
            <div class="card-body">
                {{ include('admin/instituto/_form.html.twig', {'button_label': 'Guardar'}) }}
            </div>
        </div>
        <div class="mt-3 text-center">
            {{ include('admin/instituto/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}