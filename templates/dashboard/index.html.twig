{% extends 'base.html.twig' %}

{% block title %}Dashboard index{% endblock %}


    {% block panelTitle %}
        Sistema de Administración de Dream Ingles
    {% endblock %}
    {% block body %}
{#        <div class="botones mt-4">
            <button class="btn btn-primary" type="button" data-bs-toggle="collapse" data-bs-target="#deudas" aria-expanded="false" aria-controls="deudas">
                Deudas
            </button>
            <button class="btn btn-primary" type="button" data-bs-toggle="collapse" data-bs-target="#pagos" aria-expanded="false" aria-controls="pagos">
                Últimos Pagos
            </button>
        </div>#}
        {#<ul class="nav nav-tabs" id="myTab" role="tablist">
            <li class="nav-item" role="presentation">
                <button class="nav-link active" id="deudas" data-bs-toggle="tab" data-bs-target="#deudas" type="button" role="tab" aria-controls="deudas" aria-selected="true">Deudas</button>
            </li>
            <li class="nav-item" role="presentation">
                <button class="nav-link" id="pagos" data-bs-toggle="tab" data-bs-target="#pagos" type="button" role="tab" aria-controls="pagos" aria-selected="false">Últimos Pagos</button>
            </li>#}
            {#<li class="nav-item" role="presentation">
                <table>
                    <tr>
                        <td colspan="4">
                            <form method="get" action="{{ path('dashboard_index') }}">
                                <div class="input-group mb-3">
                                    <input name="action" type="hidden" class="form-control" value="pagos">
                                    <input name="busqueda" type="text" class="form-control" placeholder="Buscar por Nombre y Apellido" value="{{ busqueda }}" aria-label="Recipient's username" aria-describedby="button-addon2">
                                    <button class="btn btn-outline-secondary" type="submit" id="button-addon2"><i class="bi bi-search"></i></button>
                                </div>
                            </form>
                        </td>
                        <td colspan="1">
                            <div class="input-group mb-3">
                                <a class="btn btn-default" href="{{ path('dashboard_index', {'action': 'pagos', 'busqueda': ''}) }}">todos</a>
                            </div>
                        </td>
                    </tr>
                </table>
            </li>#}
        {#</ul>
        #}
{#        <div class="mt-4">
            <div class="tab-pane fade show active" id="deudas" role="tabpanel" aria-labelledby="deudas">
                <table class="table h-50 table-striped table-hover">
                    <thead>
                    <tr class="cabecera">
                        <th>Nombre</th>
                        <th></th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for deudor in deudores %}
                        <tr>
                            <td>{{ deudor.nombre }} {{ deudor.apellido }}</td>
                            <td>
                                <a class="btn btn-primary" href="{{ path('app_alumnos_pagos_index', {'alumno': deudor.id}) }}">Ingresar pago</a>
                                <a class="btn btn-secondary" href="{{ path('app_alumnos_pagos_new', {'alumno': deudor.id}) }}">Ver pagos</a>
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="3">no records found</td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
            <div class="tab-pane fade " id="pagos" role="tabpanel" aria-labelledby="pagos">
                <table class="table h-50">
                    <thead>
                    <tr class="cabecera">
                        <th>Fecha</th>
                        <th>Monto</th>
                        <th>Mes</th>
                        <th>Año</th>
                        <th>Alumno</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for alumnos_pago in alumnos_pagos %}
                        <tr>
                            <td>{{ alumnos_pago.fecha ? alumnos_pago.fecha|date('Y-m-d') : '' }}</td>
                            <td>{{ alumnos_pago.monto }}</td>
                            <td>{{ alumnos_pago.mes }}</td>
                            <td>{{ alumnos_pago.ano }}</td>
                            <td>{{ alumnos_pago.alumno.nombre }} {{ alumnos_pago.alumno.apellido }}</td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="3">no records found</td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>#}
    <div class="mt-4">
        <div class="mt-3 mb-1 row">
            <div class="col-6">
                <ul class="nav nav-tabs" id="myTab" role="tablist">
                    <li class="nav-item" role="presentation">
                        <button class="nav-link active" id="home-tab" data-bs-toggle="tab" data-bs-target="#home" type="button" role="tab" aria-controls="home" aria-selected="true">Deudas</button>
                    </li>
                    <li class="nav-item" role="presentation">
                        <button class="nav-link" id="profile-tab" data-bs-toggle="tab" data-bs-target="#profile" type="button" role="tab" aria-controls="profile" aria-selected="false">Últimos Pagos</button>
                    </li>
                    <li class="nav-item" role="presentation">
                        <button class="nav-link" id="cursos-tab" data-bs-toggle="tab" data-bs-target="#cursos" type="button" role="tab" aria-controls="cursos" aria-selected="false">Cursos</button>
                    </li>
                </ul>
            </div>
            <div class="col-4 pt-1">
                <form method="get" action="{{ path('dashboard_index') }}">
                    <div class="input-group ">
                        <input name="action" type="hidden" class="form-control" value="pagos">
                        <input name="busqueda" type="text" class="form-control" placeholder="Buscar por Nombre y Apellido" value="{{ busqueda }}" aria-label="Recipient's username" aria-describedby="button-addon2">
                        <button class="btn btn-outline-secondary" type="submit" id="button-addon2"><i class="bi bi-search"></i></button>
                    </div>
                </form>
            </div>
            <div class="col-2">
                <div class="input-group mb-3">
                    <a class="btn btn-default" href="{{ path('dashboard_index', {'action': 'pagos', 'busqueda': ''}) }}">todos</a>
                </div>
            </div>
        </div>
        <div class="tab-content" id="myTabContent">
            <div class="tab-pane fade show active" id="home" role="tabpanel" aria-labelledby="home-tab">
                <table class="table h-50 table-striped table-hover">
                    <thead>
                    <tr class="cabecera">
                        <th>Nombre y Apellido</th>
                        <th>Acciones</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for deudor in deudores %}
                        <tr>
                            <td>{{ deudor.nombre }} {{ deudor.apellido }}</td>
                            <td>
                                <a class="btn btn-primary" href="{{ path('app_alumnos_pagos_index', {'alumno': deudor.id}) }}">Ingresar pago</a>
                                <a class="btn btn-secondary" href="{{ path('app_alumnos_pagos_new', {'alumno': deudor.id}) }}">Ver pagos</a>
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="3">no records found</td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
            <div class="tab-pane fade" id="profile" role="tabpanel" aria-labelledby="profile-tab">
                <table class="table h-50">
                    <thead>
                    <tr class="cabecera">
                        <th>Fecha</th>
                        <th>Monto</th>
                        <th>Mes</th>
                        <th>Año</th>
                        <th>Alumno</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for alumnos_pago in alumnos_pagos %}
                        <tr>
                            <td>{{ alumnos_pago.fecha ? alumnos_pago.fecha|date('Y-m-d') : '' }}</td>
                            <td>{{ alumnos_pago.monto }}</td>
                            <td>{{ alumnos_pago.mes }}</td>
                            <td>{{ alumnos_pago.ano }}</td>
                            <td>{{ alumnos_pago.alumno.nombre }} {{ alumnos_pago.alumno.apellido }}</td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="3">no records found</td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
            <div class="tab-pane fade" id="cursos" role="tabpanel" aria-labelledby="cursos-tab">
                <table class="table h-50 table-striped table-hover">
                    <thead>
                        <tr class="cabecera">
                            <th>Nombre</th>
                            <th>Alumnos Activos</th>
                        </tr>
                    </thead>
                    <tbody>
                    {% for curso in cursos %}
                        <tr>
                            <td>{{ curso.nombre }}</td>
                            <td>
                                <a href="{{ path('app_alumno_index', {'activo': 1}) }}&cursoSelected={{ curso.id }}">
                                    {{ curso.alumnosActivos | length }}
                                </a>
                                {#<a class="btn btn-primary" href="{{ path('app_alumnos_pagos_index', {'alumno': deudor.id}) }}">Ingresar pago</a>
                                <a class="btn btn-secondary" href="{{ path('app_alumnos_pagos_new', {'alumno': deudor.id}) }}">Ver pagos</a>#}
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="3">no records found</td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
    {% endblock %}

{% block javascripts %}
    {{ parent() }}
    {#<script>
        $(document).ready(function(){
            var myCollapse = document.getElementById('{{ action }}' );
            var bsCollapse = new bootstrap.Collapse(myCollapse, {
                toggle: false
            })
            bsCollapse.toggle();
        })

    </script>#}
{% endblock %}

