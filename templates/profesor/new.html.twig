{% extends 'base.html.twig' %}

{% block title %}Nuevo Profesor{% endblock %}

{% block body %}
    <div class="editForms">
        <h1>Nuevo Profesor</h1>
        <div class="row">
            {{ include('profesor/_form.html.twig', {'button_label': 'Guardar'}) }}
        </div>
        <br><br>
        <div class="row">
            <a class="btn btn-primary" href="{{ path('app_profesor_index') }}">Volver</a>
        </div>
    </div>
{% endblock %}
